using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class posicaodesentar : MonoBehaviour
{
    // Start is called before the first frame update
    public int numero;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    

}
